/* 
            (1)
function hi(name) {
    console.log('hi, ${name}`)
                      (2)
}
   (3)
hi("Bruce");

1.is the parameter the function is holding- *what system is asking for*
2. how we refer to the parameter *what the computer will say back* 
3. is the argument *the answer the user gives*
*/

function firstLast(firstName, lastName) {
    
}